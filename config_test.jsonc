// Expanded JSONC configuration for tests
{
  // Top-level application settings
  "Heading": "east",         // override default "south"
  "Speed": 99,                // override default 42
  "SecretKey": "file-secret", // example secret from file

  // Nested database configuration
  "Database": {
    "Host": "db.from.file",   // override default "localhost"
    "Ports": [15432, 13306,],  // trailing comma allowed in JSONC
    "Encrypt": true,
    "Auth": {
      "User": "fileuser",
      "Password": "filepassword",
    },
  },
}

